# 手机性能测试工具 (lperf) 依赖列表
# 目前主要使用 Python 标准库，以下为未来可能扩展的依赖

# 数据可视化
matplotlib>=3.5.0
numpy>=1.22.0
pandas>=1.4.0

# 图表生成
plotly>=5.0.0

# iOS 设备支持 - 使用正确的库名
libimobiledevice>=1.3.0

# 命令行界面美化
click>=8.0.0
colorama>=0.4.4

# 数据导出为 Excel - 使用正确的库名
openpyxl>=3.0.0
xlsxwriter>=3.0.0

# 日志记录和错误处理
loguru>=0.6.0

# 配置管理
pyyaml>=6.0.0

# 网络请求（用于未来扩展）
requests>=2.28.0

# 桌面平台支持
